## 1. Angular Route Redirects

- [x] 1.1 Add exact-match Angular redirects for `/my-bookmarks` to `/dashboard?tab=bookmarks` and `/my-notes` to `/dashboard?tab=notes`, preserving the existing lazy-loaded nested routes; verify the route configuration uses full path matching.

